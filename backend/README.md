# Animated Resume Analytics Builder — Backend

A scalable Express.js + TypeScript backend scaffold with MongoDB persistence and API versioning.

## Setup

1. Install dependencies:

```bash
cd backend
npm install
```

2. Create an `.env` file based on `.env.example`.

3. Start development server:

```bash
npm run dev
```

4. Build for production:

```bash
npm run build
```
```